{
  "name": "health-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private health tracker engine for NanoClaw integration.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
